angular.module('phoneApp')

.controller('tPhotoCate', function (
    $scope, 
    $state, 
    $stateParams, 
    $location, 
    $ionicViewSwitcher,
    widget
){

    $scope.go = function (e) {
        // body...
    };

    // $ionicViewSwitcher.nextDirection('forward');

    
});